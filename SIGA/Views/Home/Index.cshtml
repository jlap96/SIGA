@model User

@{
    ViewData["Title"] = "Inicio";
}

<link rel="stylesheet" href="~/css/Home.css" asp-append-version="true" />
<div class="welcome">
    <h3>BIENVENIDO</h3> 
    <h4>@DateTime.Now.ToString("dd/MM/yyyy")</h4>
    <h4 class="userName">Direccion General</h4>
</div>
